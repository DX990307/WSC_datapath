\section{Results}
\label{sec:results}

\subsection{Overall Performance}

Figure~\ref{fig:cupath-performance} compares CuPath with Baseline across the
fourteen workloads. CuPath achieves a 1.492$\times$ geometric-mean speedup.
MM, FWS, and KM obtain the largest gains at 6.180$\times$, 4.341$\times$,
and 2.501$\times$, respectively. Local workloads also benefit. BT and FWT
improve by 1.635$\times$ and 1.645$\times$, while RELU improves by
1.149$\times$. I2C improves by 1.215$\times$ through the remote path. Eight
workloads improve by more than 1\%, while the remaining six stay within 1\%
of Baseline. SPMV is the only strict regression and remains within 0.1\% of
Baseline. CuPath therefore provides large gains when the memory path exposes
pairing, aggregation, or reuse opportunities without materially penalizing
workloads that expose few such opportunities.

\begin{figure}[t]
  \centering
  \includegraphics[width=\columnwidth]{../../akkalat/results/2026-07-23-23-41-05-sampled-validation/cupath_baseline_complete_speedup.png}
  \caption{Performance normalized to Baseline. Bars exceeding 2$\times$ are
  clipped and annotated with their actual speedups. GMEAN is the geometric
  mean across all fourteen workloads.}
  \label{fig:cupath-performance}
\end{figure}

\subsection{Ablation Study}

Figure~\ref{fig:cupath-ablation} isolates the three CuPath transformations.
Remote Aggregation is the strongest standalone transformation and achieves a
1.209$\times$ geometric-mean speedup. Remote Reuse and Local Pairing achieve
1.156$\times$ and 1.108$\times$, respectively. The different geometric means
reflect the opportunity exposed at each memory-path stage rather than a
uniform benefit across all workloads. Remote Aggregation is most effective
when remote reads expose same-line duplication or same-page grouping. FWS and
KM consequently reach 2.222$\times$ and 2.462$\times$. Remote Reuse is more concentrated and
provides its largest gain on MM at 5.451$\times$, where later requests can be
served by the requesting-GPM L2 instead of crossing the wafer again. I2C also
benefits from Remote Reuse at 1.097$\times$.

Local Pairing targets a different request population. BT and FWT improve by
1.635$\times$ and 1.645$\times$, and RELU improves by 1.149$\times$. For these
workloads, the complete configuration closely follows Local Pairing because
little remote traffic reaches the other two transformations. In contrast,
workloads that expose fewer usable pairing opportunities remain close to
Baseline. The standalone results therefore show that no single transformation
explains CuPath's gains across the complete workload set.

Complete CuPath reaches 1.492$\times$ and exceeds every standalone
configuration in geometric mean. Workloads exposing several opportunities
benefit from their composition. FWS improves from 2.222$\times$ with Remote
Aggregation to 4.341$\times$ with Complete. MM combines a 2.341$\times$
Remote Aggregation result with a 5.451$\times$ Remote Reuse result and reaches
6.180$\times$ with Complete. I2C reaches 1.215$\times$, compared with
1.097$\times$ for its strongest standalone configuration. When one
opportunity dominates, Complete remains close to the corresponding standalone
result. KM, for example, reaches 2.501$\times$ with Complete and
2.462$\times$ with Remote Aggregation. The speedups are complementary but not
additive because every stage operates on the requests that remain after
earlier stages have removed or combined work.

\begin{figure}[t]
  \centering
  \includegraphics[width=\columnwidth]{../../akkalat/results/2026-07-23-23-41-05-sampled-validation/cupath_latest_ablation_pasta_style.png}
  \caption{Ablation of the three CuPath transformations. Remote Aggregation,
  Remote Reuse, and Local Pairing achieve geometric-mean speedups of
  1.209$\times$, 1.156$\times$, and 1.108$\times$, respectively, while
  Complete reaches 1.492$\times$. Performance is normalized to Baseline. Bars
  exceeding 2$\times$ are clipped, with labels showing the actual speedups.}
  \label{fig:cupath-ablation}
\end{figure}

\subsection{Large-Model Workloads}

Figure~\ref{fig:cupath-large-models} evaluates CuPath on BERT, GPT, and
ResNet.  CuPath accelerates all three workloads, with substantially larger
gains for BERT and GPT than for ResNet.  Transformer execution repeatedly
exposes adjacent accesses, concurrent remote requests, and remote data reuse,
allowing CuPath to remove or combine work at several points along the memory
path.  ResNet exposes fewer of these opportunities, but still benefits from
the same mechanisms.  The results show that CuPath extends beyond traditional
GPU kernels and remains effective across both transformer and convolutional
workloads.

\begin{figure}[t]
  \centering
  \includegraphics[width=\columnwidth]{../../akkalat/results/2026-07-24-05-33-37-cupath-llm/cupath_large_model_speedup.pdf}
  \caption{Normalized performance of CuPath on BERT, GPT, and ResNet. CuPath
  achieves speedups of 6.44$\times$, 6.58$\times$, and 1.27$\times$,
  respectively, over the normalized Baseline.}
  \label{fig:cupath-large-models}
\end{figure}

\subsection{Removing Work Along the Path}

Figure~\ref{fig:cupath-work} explains the speedup in terms of work that no
longer reaches a downstream component.  Weighted across the fourteen workloads,
Complete deduplicates 9.3\% of remote reads, reduces remote request packets by
52.9\% through aggregation, and eliminates 29.3\% of repeated wafer
traversals.  The different columns peak on different applications, which
supports the path-oriented design because no single boundary exposes all three
reductions.

CuPath does not obtain these gains by increasing aggregate memory traffic.
Complete reduces physical DRAM reads from 39.25 to 38.71 million (1.38\%) and
writes from 20.83 to 20.68 million (0.71\%).  The sample-weighted local
L2-miss-to-DRAM-issue latency falls from 38.12~ns in Baseline to 20.34~ns over
the ordinary and Filter-negative Complete issue populations.  We report these
populations separately in the machine-readable results; the comparison is not
an assumption that every miss takes the fast path.

\begin{figure}[t]
  \centering
  \includegraphics[width=\columnwidth]{../../akkalat/results/2026-07-22-baseline-complete-observation/cupath_work_reduction.png}
  \caption{Percentage of work removed at successive remote-path boundaries.
  The bottom row is weighted by the corresponding request count.}
  \label{fig:cupath-work}
\end{figure}

\subsection{Typed Filter Effectiveness}

Figure~\ref{fig:filter-effectiveness} summarizes the exact accesses removed by
reliable negative results across the fourteen Complete runs, while
Figure~\ref{fig:filter-effectiveness-workloads} shows the workload-level
distribution. The Filter bypasses
32.79 million of 135.34 million eligible L2 tag-pipeline lookups, 89.52
million of 98.88 million requester-RDMA request-table searches, and 13.28
million of 47.71 million exact requester-L2 probes. These reductions
correspond to 24.2\%, 90.5\%, and 27.8\% of the respective opportunities.
The three populations arise at different path stages, showing that the shared
metadata is exercised by local lookup, remote aggregation, and remote reuse
rather than serving as an unused auxiliary structure.

Across 434.39 million Filter queries, only 33,993 possible matches are false
positives. False positives therefore account for 0.0078\% of all queries, and
the standard false-positive rate among verified absent-key queries is
0.0148\%. Table~\ref{tab:filter-effectiveness} breaks down activity by type.
No insertion fails for active residency, RDMA, or paired-access state, even
though the most heavily used physical Filter reaches 89.9\% peak occupancy.
Low-priority remote-history and demand-pattern entries have insertion-failure
rates of 42.3\% and 10.3\%. These failures suppress an optional reuse or
pairing opportunity and fall back to the demand path without affecting
correctness.

We normalize lookup stalls by completed queries plus stall events, and update
stalls by recorded insertions and deletions plus stall events. The resulting
weighted contention ratios are 0.264\% and 0.049\%. Contention is not uniform,
however, because RELU reaches a 58.5\% lookup ratio during its concentrated
local-access stream. The weighted result shows that contention is uncommon
over the full suite, while the workload maximum prevents us from claiming
that the interface is contention-free for every access pattern.

\begin{figure}[t]
  \centering
  \includegraphics[width=\columnwidth]{../../akkalat/results/2026-07-23-23-41-05-sampled-validation/cupath_filter_effectiveness.pdf}
  \caption{Exact metadata accesses bypassed by reliable negative results from
  the shared typed Cuckoo Filter. Results aggregate the fourteen Complete runs
  using eligible exact accesses as the denominator. Labels report the bypass
  rate and number of bypassed accesses.}
  \label{fig:filter-effectiveness}
\end{figure}

\begin{figure}[t]
  \centering
  \includegraphics[width=\columnwidth]{../../akkalat/results/2026-07-23-23-41-05-sampled-validation/cupath_filter_effectiveness_by_benchmark.pdf}
  \caption{Per-workload exact accesses bypassed by reliable Filter negatives.
  \emph{ALL} aggregates the fourteen Complete runs using the corresponding
  eligible-access count. Missing bars indicate no eligible access.}
  \label{fig:filter-effectiveness-workloads}
\end{figure}

\begin{table}[t]
  \centering
  \caption{Per-type activity of the shared typed Filter. FPR is measured only
  over absent-key queries. ``--'' denotes no absent-key query population.}
  \label{tab:filter-effectiveness}
  \scriptsize
  \setlength{\tabcolsep}{3.2pt}
  \begin{tabular}{lrrr}
    \toprule
    Metadata type & Queries & FPR & Insert failure \\
    \midrule
    L2-Resident        & 241.05M & 0.0413\% & 0\% \\
    RDMA-In-Flight     & 110.66M & 0.0001\% & 0\% \\
    Remote-History     &  59.53M & 0.0036\% & 42.3\% \\
    Demand-Pattern     &  11.81M & --        & 10.3\% \\
    Adjacent-In-Flight &  11.35M & 0.0001\% & 0\% \\
    \bottomrule
  \end{tabular}
\end{table}

\subsection{M1: Cuckoo-Filter-Guided Local Pairing}

M1 is a supporting transformation rather than the dominant source of
speedup.  It issues 261,615 local candidates over the full suite: 252,850
reach the DRAM-issue point and 8,765 lose a race to an ordinary demand;
176,447 are eventually useful.  Although these issued candidates are
additional logical accesses, exact MSHR merging and demand timing leave total
physical reads 0.35\% lower than Baseline and increase L2 MSHR-full cycles by
only 0.22\%.  FIR is the exception: M1 alone is 0.898$\times$ and increases
physical reads and writes, so we do not claim that local prediction is
universally beneficial.

The coupling experiment isolates why the Filter is still useful for mostly
local workloads.  Filter-only definite-miss elimination provides
1.0145$\times$ geometric mean, while ungated prediction provides only
1.0059$\times$.  Coupling the same predictor to \textsc{Pattern},
\textsc{Resident}, and \textsc{Pending} reaches 1.0188$\times$.  It removes
34.7\% of issued candidates while retaining 96.7\% of useful candidates,
raising useful-per-issued accuracy from 24.0\% to 35.5\%.  Additional DRAM
reads fall from 6,206 to 5,578 and demand-delay exposure events from 145,466
to 33,066.  Predictor-only records candidates but is exactly 1.0000$\times$,
confirming that observation itself is not counted as timing benefit.

Replacing Cuckoo membership with exact metadata changes the same screen only
from 1.0188$\times$ to 1.0192$\times$.  The Cuckoo design observes four false
positives in 273,146 queries (0.0015\%).  Thus the Filter does not move data or
create prediction benefit by itself; it compactly rejects avoidable work and
tracks the request lifecycle with little loss relative to exact metadata.

\subsection{M2: Cuckoo-Filter-Guided Remote Aggregation}

M2 is the largest standalone contributor.  Complete records 4.24 million
real demands merged by exact RDMA line state and 2.52 million predicted lines
piggybacked into existing home-GPM/page packets.  Because a real demand can take
over a candidate before batch insertion, the aggregate counters do not expose
the intersection of these two populations; we therefore do not relabel every
useful prediction as an avoided packet.  The exact merge and wire-packet
counters independently establish the reductions.

\subsection{M3: Cuckoo-Filter-Guided Remote Reuse}

M3 bypasses 23.41 million exact requesting-GPM L2 probes when \textsc{Resident}
and \textsc{Seen} reliably prove absence, 42.6\% of eligible unique-request
probe opportunities.  The remaining probes produce 27.23 million physical
L2 hits and satisfy 28.21 million logical responses through exact waiter
fanout.  M3 installs 7.06 million remote-clean lines in the existing L2;
0.996 million are observed to retire unused, a 14.1\% lower bound because
lines still resident at termination are unclassified.  Invalid ways absorb
5.35 million fills and older remote-clean lines absorb the remaining 1.71
million; no ordinary local-clean line is displaced.  The largest
workload-level sum of peak occupancies reaches 37.5\% of wafer L2 lines, a
conservative non-simultaneous upper bound rather than an average footprint.
Of the unused retirements, 160,257 carry candidate provenance and remove the
associated \textsc{Pattern}.  A useful patterned hit instead keeps
\textsc{Pattern} eligible; the aggregate counter does not isolate that subset
from all requesting-GPM L2 hits.

\subsection{Limitations}

The long runs expose a limitation hidden by the short screen. Across the
fourteen Complete runs, active residency, RDMA, and paired-access state have
zero insertion failures. Capacity pressure instead rejects 5.57 million of
13.17 million low-priority remote-history insertions and 2.85 million of
27.59 million demand-pattern insertions. These failures suppress optional
reuse or pairing and cannot change demand correctness, but they bound
coverage. The modest
1.187$\times$ Remote-group speedup and M1's FIR regression likewise show that
CuPath is constrained by available aggregation and safe idle opportunities,
not by an unlimited speculative upper bound.

A controlled simulator-runtime screen uses seven representative workloads,
all five configurations, 192 drained workgroups, one host worker, and the
same binary. Normalized by simulated GPU time, Complete costs
1.190$\times$ Baseline. This value is simulator overhead rather than hardware
performance.
